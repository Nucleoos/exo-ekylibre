<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h1>Les cuves</h1>
      <%= link_to("Ajouter une cuve", new_tank_path) %>
      <div class="">
        <% @tanks.each do |tank| %>
          <div class="">
            <%= link_to(tank.name, tank_path(tank)) %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
